class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int altitude = 0;
        int highest = 0;

        for (int h : gain) {
            altitude += h;
            highest = max(highest, altitude);
        }

        return highest;
    }
};